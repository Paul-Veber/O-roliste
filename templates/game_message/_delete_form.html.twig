<form method="post" action="{{ path('game_message_delete', {'id': game_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game_message.id) }}">
    <button class="btn">Delete</button>
</form>
